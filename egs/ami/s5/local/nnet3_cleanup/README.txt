# this directory contains some scripts that provide a simpler alternate path to the scripts in
# nnet3/.  These scripts are designed to be used with
# local/run_cleanup_segmentation.sh, which cleans the data to only those parts that align easily
# to the transcript.  This removes the necessity
